<h2 class="font-1-2">Welcome back. 👋</h2>
<h2 class="font-1-2">You've been missed!</h2>

<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "mt-10"}) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                label: false,
                # autofocus: true,
                placeholder: "john.doe@email.com",
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                label: false,
                placeholder: "•••••••",
                input_html: { autocomplete: "current-password" } %>
    <%#= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Log in", class: "form-submit" %>
  </div>
<% end %>

